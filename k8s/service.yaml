apiVersion: v1
kind: Service
metadata:
  name: journal-api
  labels:
    app: journal-api
spec:
  selector:
    app: journal-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
