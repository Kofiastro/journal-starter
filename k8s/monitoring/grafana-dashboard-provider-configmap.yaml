apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
  labels:
    app: grafana
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: Journal API Dashboards
        orgId: 1
        folder: Journal API
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards
